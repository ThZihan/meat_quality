# Meat Quality Monitoring System - Python Dependencies

# Core Streamlit framework
streamlit>=1.28.0

# Flask for camera feed server
Flask>=3.0.0

# Data manipulation and analysis
pandas>=2.0.0
numpy>=1.24.0

# Visualization libraries
plotly>=5.17.0
seaborn>=0.12.0
matplotlib>=3.7.0

# Additional utilities
altair>=5.0.0

# HTTP client for upload and camera integrations
requests>=2.31.0

# MQTT Client for ESP32 communication (legacy)
paho-mqtt>=1.6.1

# Environment variable loading
python-dotenv>=1.0.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0

# GPIO control for illumination LED (PWM on GPIO 18)
# gpiozero is pre-installed on Raspberry Pi OS Bookworm+.
# For older images, install via: sudo apt install python3-gpiozero
gpiozero>=2.0.0; platform_machine == "aarch64" or platform_machine == "armv7l"

# Database (SQLite is built-in, but these are for convenience)
# No additional packages needed for SQLite

# Date/time utilities
python-dateutil>=2.8.2

# System Requirements for Camera Module 3
# ========================================
# The following packages must be installed via apt (system package manager), not pip:
#
# 1. libcamera-apps - Provides rpicam-vid and rpicam-still for Camera Module 3
#    sudo apt install libcamera-apps
#
# 2. libcamera-tools - Additional libcamera utilities
#    sudo apt install libcamera-tools
#
# 3. v4l-utils - Video4Linux utilities for camera management
#    sudo apt install v4l-utils
#
# Installation command (all at once):
# sudo apt update && sudo apt install -y libcamera-apps libcamera-tools v4l-utils
#
# Note: The legacy picamera library is NOT used. This system uses the modern
# libcamera stack with Camera Module 3 support.

# BLE ingest (ESP32 -> Pi). dbus-fast is pulled in by bleak on Linux and is
# what talks to BlueZ over D-Bus.
bleak>=1.0.0
